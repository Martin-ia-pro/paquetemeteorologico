---
title: "introduccion-al-paquete"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{introduccion-al-paquete}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

```{r setup}
library(paquetemeteorologico)
```

## Introducción al paquete paquetemeteorologico

El paquete `paquetemeteorologico` proporciona herramientas para la lectura,manipulacion y visualizacion de los datos de estaciones meteorológicas. En esta introducción, exploraremos algunas de las funcionalidades basicas del paquete.

### Descarga y lectura de datos meteorológicos

El paquete incluye funciones para descargar y leer datos de estaciones meteorológicas A continuación, se muestra un ejemplo de cómo descargar y leer datos meteorológicos utilizando la función `leer_estaciones()`.

```{r, include = FALSE}
#Definimos rutas temporales para el ejemplo
ruta_temp1 <- file.path(tempdir(), "NH0437.csv")
ruta_temp2 <- file.path(tempdir(), "NH0910.csv")
```


```{r}
# Ejemplo de cómo descargar y leer datos de una estación
estacion_1 <- leer_estaciones("NH0437", ruta_temp1)
estacion_2 <- leer_estaciones("NH0910", ruta_temp2)
```

### Resumen de datos meteorológicos

El paquete también ofrece funciones para resumir datos meteorológicos. A continuación, se muestra un ejemplo de como crear un resumen de la temperatura promedio mensual utilizando la función `tabla_resumen_temperatura()`.

```{r}
# Crear un resumen de la temperatura promedio mensual para las estaciones leidas

tabla_resumen_temperatura(estacion_1)

```

### Visualización de datos meteorológicos

El paquete también ofrece funciones para visualizar datos meteorológicos. A continuación, se muestra un ejemplo de como graficar la temperatura mensual utilizando la función `graficar_temperatura_mensual()`.

```{r}
# Graficar la temperatura mensual de una estacion específica

graficar_temperatura_mensual(estacion_1,c("green", "blue", "red","yellow","purple"),"Temperatura Promedio Mensual Estación 1")
```

```{r}
graficar_temperatura_mensual(estacion_2,c("purple", "pink", "lightblue","orange","brown"),"Temperatura Promedio Mensual Estación de Argentina-Rosario")
```


### Documentación adicional

Podes explorar la documentación de cada función en detalle usando la ayuda de R:

-   `?leer_estaciones`
-   `?tabla_resumen_temperatura`
-   `?graficar_temperatura_mensual`
